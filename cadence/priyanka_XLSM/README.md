Mostly work of Priyanka Jaiswal through Upwork

After dumping from Assembler's Detail-Transpose view, as CSV:

$ gen_xlsm_from_transpose.sh run_allN.csv # tells you the name of the final output file..
$ python3 ~/python/gen_PJ_index_xlsm.py run_allN_fixed_eng.csv

These are old. It is best to give the files to chatGPT or Claude and get them updated per your needs
